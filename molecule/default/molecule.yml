---
driver:
  name: docker

platforms:
  - name: instance
    image: docker.io/ubuntu:22.04
    command: /sbin/init
    privileged: true
    pre_build_image: true

provisioner:
  name: ansible
  playbooks:
    converge: ${MOLECULE_PLAYBOOK:-converge.yml}

verifier:
  name: ansible
