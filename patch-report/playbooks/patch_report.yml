- name: Collect pending patches
  hosts: ubuntu
  become: true
  gather_facts: true
  roles:
    - patch_scan

- name: Generate reports and send email
  hosts: localhost
  gather_facts: false
  roles:
    - report_generate
    - mail_send
