# GitOps

Argo CD, drift, sync failures, and the awkward question of secrets in a world
where the desired state is public.

## Written

*None yet.*

## Planned

| Case study | Type |
|---|---|
| Argo CD configuration drift and self-heal fighting an operator | Troubleshooting |
| Sync failure: hooks, ordering, and resources that never become healthy | Troubleshooting |
| Rollback in a GitOps world — revert, not `kubectl` | Incident |
| Multi-environment promotion with one source of truth | Architecture |
| Secrets management: External Secrets, SOPS, and sealed secrets compared | Security |
