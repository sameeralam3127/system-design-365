*📦 Package Update Report for* `{{ ansible_hostname }}`

*🖥️ System Information*
• OS: `{{ ansible_distribution }} {{ ansible_distribution_version }}`
• Hostname: `{{ ansible_hostname }}`
• Package Manager: `{{ package_manager }}`

*📡 Repository Status*
{% if repos_configured -%}
✅ Repositories are *configured*
{%- else -%}
❌ Repositories are *NOT configured*
{%- endif %}

*📋 Updates*
{% if updates_available -%}
• Total: `{{ updates_list | length }}`
• Security: `{{ security_updates_count }}`
{%- else -%}
ℹ️ No updates available
{%- endif %}

*⚙️ Actions Taken*
{% if not dry_run -%}
  {% if repos_configured and updates_available -%}
    {% if security_updates_only -%}
✅ `{{ security_updates_count }}` security updates applied
    {%- else -%}
✅ `{{ updates_list | length }}` updates applied
    {%- endif %}
  {% elif not updates_available -%}
ℹ️ No updates available
  {%- else -%}
❌ Failed to apply updates (repo issues)
  {%- endif %}
{%- else -%}
🛑 Dry run enabled — no changes made
{%- endif %}

*🔁 Reboot Status*
{% if reboot_required -%}
  {% if reboot_if_required -%}
🔄 *REBOOT INITIATED*
  {%- else -%}
⚠️  *REBOOT REQUIRED* (not initiated)
  {%- endif %}
{%- else -%}
✅ No reboot needed
{%- endif %}
