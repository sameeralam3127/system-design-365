---
title: {{title}}
description:
tags: []
author: Sameer Alam
created: {{date}}
updated: {{date}}
status: draft
---

## Overview

Write your content here. Markdown, Mermaid diagrams, admonitions, emoji
shortcodes, and tables are all supported.

> [!TIP]
> Run `sd365 serve` to preview while you write.

> [!EXAMPLE]- Everything the renderer supports
> Admonition kinds: `NOTE` `INFO` `TIP` `IMPORTANT` `SUCCESS` `QUESTION`
> `WARNING` `DANGER` `BUG` `EXAMPLE` `QUOTE` `ABSTRACT`.
>
> Add text after the marker for a custom title; add `-` to start collapsed
> (like this one) or `+` for collapsible but open.
>
> Emoji shortcodes work in prose — `:rocket:` renders as :rocket: — and are
> left alone inside code.

## Notes

- Set `status: published` when this is ready; drafts render as "not written yet".
- Tags in the frontmatter become links to `/tags/<tag>/` automatically.
- Delete the sections you don't need — nothing here is required.
