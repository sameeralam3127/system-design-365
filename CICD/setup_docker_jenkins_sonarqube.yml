---
- name: Setup Docker, Jenkins and SonarQube
  hosts: ubuntu_servers
  become: yes
  vars:
    ansible_python_interpreter: /usr/bin/python3
  roles:
    - common
    - docker
    - jenkins
    - sonarqube 