---
- name: Install EPEL repository (RHEL/CentOS 7)
  yum:
    name: "{{ epel_packages }}"
    state: present
  when: ansible_distribution_major_version == '7'

- name: Install Nginx and dependencies (RHEL/CentOS)
  yum:
    name: "{{ nginx_packages }}"
    state: present
